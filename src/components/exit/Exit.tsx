import React from 'react';
import { Layout, Input, Divider, Typography } from 'antd';

const { Search } = Input;
const { Title } = Typography;

const Exit: React.FC = () => {

    return (
        <Layout>
        </Layout>
    )
}

export default Exit;
